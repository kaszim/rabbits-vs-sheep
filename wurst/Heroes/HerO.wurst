package HerO
import ObjectIdGenerator
import UnitObjEditing

public constant HERO_ID = compiletime(HERO_ID_GEN.next())

@compiletime function createHero()
    new HeroDefinition(HERO_ID, 'Nalc')
    ..setName("herO")

