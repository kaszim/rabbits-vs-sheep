package HeroEntity
import EntityManagement

public class HeroEntity extends UnitEntity

    construct(player p, vec2 pos, int id)
        super(createUnit(p, id, pos, angle(0)))

    ondestroy
        print("Hero Destroyed")